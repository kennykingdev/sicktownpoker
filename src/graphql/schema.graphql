type Mutation {
  createPlayer(input: PlayerCreationInput!): Player!
  deletePlayer(playerId: Int!): Player!
  updatePlayer(input: PlayerUpdateInput!): Player!
}

type Player {
  email: String
  firstName: String!
  fullName: String!
  id: ID!
  lastName: String!
  phone: String
  referralCount: Int!
  referredByPlayer: Player
  referredPlayers: [Player!]!
  shareContactInfo: Boolean!
}

input PlayerCreationInput {
  firstName: String!
  lastName: String!
}

input PlayerUpdateInput {
  email: String
  firstName: String
  id: Int!
  lastName: String
  phone: String
  referredByPlayerId: Int
  shareContactInfo: Boolean
}

type Query {
  player(id: Int!): Player!
  players: [Player!]!
}
