import { id, findByIdInput } from './shared';
import * as player from './player';
import * as tournament from './tournament';

export const validate = {
  id,
  findByIdInput,
  player,
  tournament,
};
