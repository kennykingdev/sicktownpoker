import { id, findByIdInput } from './shared';
import { player, playerUpdateInput } from './player';
import { tournament, tournamentUpdateInput } from './tournament';

export const validate = {
  id,
  findByIdInput,
  player,
  playerUpdateInput,
  tournament,
  tournamentUpdateInput,
};
